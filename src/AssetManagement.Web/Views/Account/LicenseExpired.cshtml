@using AssetManagement.Web.Helpers
@{
    ViewBag.Title = "License expired";
    var organizationName = ViewBag.OrganizationName as string;
}

<div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
        <div class="card card-kpi">
            <div class="card-header">License expired</div>
            <div class="card-body">
                <h1 class="h4 mb-3">Portal access unavailable</h1>
                @if (!string.IsNullOrWhiteSpace(organizationName))
                {
                    <p>The license for <strong>@organizationName</strong> has expired.</p>
                }
                else
                {
                    <p>Your organization's license has expired.</p>
                }
                <p class="text-muted mb-4">Please contact your platform administrator to renew the license.</p>
                <form action="@TenantUrlHelper.TenantRouteUrl(Url, ViewBag.TenantToken as string, "LogOff", "Account")" method="post" class="mb-0">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn btn-outline-secondary">Sign out</button>
                </form>
            </div>
        </div>
    </div>
</div>
